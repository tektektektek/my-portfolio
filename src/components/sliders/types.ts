export type Slide = {
    statement: string;
    customer: {
        avatar: string;
        name: string;
        designation: string;
    };
    logo?: string;
}

export type Slide1 = {
    image: string;
    slideTitle: string;
    description: string;
}
